server:
  port: 0
  tomcat:
    mbeanregistry:
      enabled: true

spring:
  application:
    name: user-service
  cloud:
    config:
      name: trend-chat-dev, user-service-dev
  config:
    import:
      - optional:configserver:${CONFIG_SERVER_URI}